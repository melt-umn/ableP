	switch (t->back) {
	default: Uerror("bad return move");
	case  0: goto R999; /* nothing to undo */

		 /* PROC monitorStatus */
;
		;
		
	case 4: /* STATE 2 */
		;
		p_restor(II);
		;
		;
		goto R999;

		 /* PROC determineStatus */
;
		;
		
	case 6: /* STATE 2 */
		;
		((P0 *)this)->_3_altStatus = trpt->bup.oval;
		;
		goto R999;
;
		;
		;
		;
		;
		;
		;
		;
			}

